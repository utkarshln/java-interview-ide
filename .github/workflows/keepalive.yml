name: Keep Render Warm
on:
  schedule:
    - cron: '*/10 * * * *'  # every 10 min
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -s https://java-interview-ide.onrender.com/api/java-version | head -c 100
