# Java Interview IDE — by Utkarsh
Free, pink-themed Java IDE for interviews. Run `node server.js` locally (requires JDK).

Local: http://localhost:5173
Deploy: see Dockerfile + render.yaml for Render/Railway free tier.
